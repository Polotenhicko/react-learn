import React from 'react';
import ReactDOM, { createRoot } from 'react-dom/client';
import { Example } from './effectHook';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Example />);
